# Заметки по логике кода

> [!IMPORTANT]
> **На будущее:** Точно забуду, поэтому записываю здесь ключевые моменты реализации.

## 1. Сортировка файлов по номеру в имени

Разбор сложной конструкции сортировки, чтобы не запутаться позже.

### Как это работает:
1. **`lambda x`** — безымянная функция, которая получает элемент `x` (файл).
2. **`:`** — разделитель, после которого идет то, что функция должна вернуть.
3. **`re.findall(r'\d+', x.name)`** — поиск по регулярке всех чисел в имени файла.
4. **`[0]`** — взятие первого найденного числа (в данном случае это основной индекс).
5. **`key=`** — сортировка выполняется по результату этой функции.

### Пример реализации:
```python
# Сортировка списка файлов на основе числа в имени
files.sort(key=lambda x: int(re.findall(r'\d+', x.name)[0]))

## 2. 🖼️ Последовательный вывод изображений

Сниппет для быстрой визуализации массивов изображений (например, после `np.load`). Полезно для отладки и проверки данных.

### 🎬 Код для анимации:
```python
for file in files:
    img = np.load(file)
    
    plt.clf()  # 🧹 Очистка предыдущего изображения
    
    plt.imshow(img)
    plt.axis('off')            # Скрыть оси координат
    plt.title(f'Показ: {file}') # Заголовок с именем файла
    
    plt.pause(0.03)            # Пауза для анимации

plt.show()
